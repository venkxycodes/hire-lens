Use `.cursor/skills/adversarial-review/SKILL.md`.

The user invoked `/adversarial-review`. Run a read-only adversarial review of the current work (branch diff, recent changes, or files the user named).

Produce the synthesized verdict from the skill. Do not edit files unless the user separately asks for fixes.

State the author's intent before spawning reviewers.
