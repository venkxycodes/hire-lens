Use `.cursor/skills/design-an-interface/SKILL.md`.

The user invoked `/design-interface`. Explore radically different interface designs for the module or API described in this conversation.

Do not implement. Present, compare, and synthesize interface options per the skill workflow.

If requirements are unclear, gather them before spawning design sub-agents.
