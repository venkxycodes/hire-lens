Use `.cursor/skills/code-simplification/SKILL.md`.

The user invoked `/simplify-code`. Simplify recently changed code for clarity without changing behavior.

Default scope: files in the current diff or the code the user pointed at. Do not broaden scope unless asked.

Run relevant tests or static checks after each incremental simplification.
