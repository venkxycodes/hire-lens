Use `.cursor/skills/deslop/SKILL.md`.

The user invoked `/deslop`. Detect and remove AI slop from the requested scope.

Default: **report** mode on **diff** scope. Switch to **apply** only when the user
says apply, fix, or auto-fix.

Read `.cursor/deslop/profile.md` and `.cursor/repo-intel.json` when present.
Run verification after any apply-mode edits.
